#include "variadic_functions.h"

/**
 * print_all - print anything
 * @str: string
 */

void print_all(const char * const format, ...)
{
	while(*format)
	{
		if (format == c)
		{

		}
		else if(format ++ i)
		{

		}
		printf();
		format++
	}
}
