#include <stdio.h>

/**
 * main - print number of arguments
 * @argc: argc
 * @argv: argv
 * Return: 0;
 */

int main(int argc, char *argv[])
{
	int i = 0;

	for (i = 0; i < argc; i++)
	{
	}
	printf("%d\n", i);
}
